# Example .env file for Docker Compose
# Copy this to .env and customize

# Authentication Secret
# This token is used for API authentication between frontend and backend
# Generate a strong random token: openssl rand -base64 32
PARENT_AUTH_SECRET=change-this-to-a-strong-random-token

# Optional: Database Configuration
# Uncomment to override defaults
# DATABASE_URL=postgres://postgres:postgres@db:5432/english_game

# Optional: Node Environment
# NODE_ENV=development
